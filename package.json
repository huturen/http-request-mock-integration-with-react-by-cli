{
  "name": "http-request-mock-integration-with-react-by-cli",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.2",
    "@testing-library/react": "^12.1.3",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^0.26.0",
    "highlight.js": "^11.4.0",
    "raw-loader": "^4.0.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-highlight": "^0.14.0",
    "react-scripts": "5.0.0",
    "react-state-proxy": "^1.4.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "mock-dev": "http-request-mock-cli -p marked -d src/mock -w \"react-scripts start\"",
    "mock-dev2": "http-request-mock-cli -p matched -d src/mock -w \"react-scripts start\"",
    "gen-proto": "http-request-mock-cli -d src/mock --proto",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "http-request-mock": "^1.6.10"
  }
}
